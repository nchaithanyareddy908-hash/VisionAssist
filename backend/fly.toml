app = "visionassist-backend"

[build]
  image = "Dockerfile"

[[services]]
  internal_port = 8000
  protocol = "tcp"
  [services.concurrency]
    type = "connections"
    soft_limit = 20
    hard_limit = 30
  [[services.ports]]
    handlers = ["http"]
    port = 80
